import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No Next.js 16, as opções experimentais mudaram de lugar ou foram oficializadas.
  // Vamos deixar vazio para o padrão estável rodar.
};

export default nextConfig;