{
  "name": "itp-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "itp_frontend"
  ],
  "scripts": {
    "build:backend": "nest build",
    "build:frontend": "cd itp_frontend && npm run build",
    "vercel-build": "npm run build:backend && npm run build:frontend"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.4.15",
    "@nestjs/typeorm": "^10.0.2",
    "express": "^4.18.2",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1",
    "pg": "^8.11.0",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.0.0",
    "typescript": "^5.7.3"
  }
}